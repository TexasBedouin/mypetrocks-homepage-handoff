# Start here

**For Amer:** send this whole package to Audos. In their Cursor, paste the contents of `CURSOR_PROMPT.md` and attach or point it at this folder.

**For the Audos team / Cursor:**
1. Read `CURSOR_PROMPT.md`. It has the step-by-step instructions.
2. Read `HANDOFF.md`. It has the background, the section order, and the copy rules.
3. Apply `homepage-redesign.patch`, or rebuild from `files/` if the patch doesn't apply cleanly.

The design this was built from is on Amer's design canvas ("MyPet.Rocks Homepage Directions", boards "FINAL part 1" and "FINAL part 2").

Full-size original images are in the separate `originals` folder Amer keeps. They're reference only; the site uses the prepared files inside `files/`.
